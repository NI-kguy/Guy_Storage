I would like to create a weight recorder UI using python. 

It needs to have these function. 
1. All data query from SQL database. 
2. Users can key in Name, Weight and date to record users daily weight. 
3. The UI will process the data. Gain/Loss using today weight reduct the yesterday weight. It must be same name.
4. Users can edit the existing data in the database. 
5. Users can remove the data from database.
6. Display graph based on the Name select. 
7. On date side, just let users select Sunday to Saturday. no need to write themselves. 
